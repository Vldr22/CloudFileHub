spring:
  kafka:
    bootstrap-servers: localhost:29092

clamav:
  host: localhost
  port: 3310

# === Logging (verbose) ===
logging:
  level:
    root: INFO
    org.resume.fileantivirusservice: DEBUG
    org.springframework.transaction: DEBUG
    org.springframework.jdbc.datasource: DEBUG
    ## software.amazon.awssdk: DEBUG